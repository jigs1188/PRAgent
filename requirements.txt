# ── Core LLM orchestration ──
langchain-core>=1.4.0
langgraph>=1.2.0

# ── LLM providers (Gemini is default; install others only if needed) ──
langchain-google-genai>=4.0.0
langchain-openai>=1.0.0           # for OpenAI / gpt-4o-mini fallback
# langchain-anthropic>=0.3.0     # uncomment for Anthropic

# ── Vector store ──
pinecone>=5.0.0

# ── Repository operations ──
gitpython>=3.1.0

# ── Go code parsing ──
tree-sitter>=0.23.0
tree-sitter-go>=0.23.0

# ── Utilities ──
requests>=2.31.0
python-dotenv>=1.0.0
tenacity>=8.0.0
